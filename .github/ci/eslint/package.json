{
  "private": true,
  "type": "module",
  "scripts": {
    "check": "cd ../../.. && npm run build && eslint --config .github/ci/eslint/eslint.config.js src tests",
    "fix": "cd ../../.. && npm run build && eslint --fix --config .github/ci/eslint/eslint.config.js src tests"
  },
  "devDependencies": {
    "@types/node": "^25.9.8",
    "eslint": "^10.11.0",
    "typescript": "^6.0.3"
  }
}
